1、语音模块：串口选择（串口4），波特率（115200），中断优先级（3），响应优先级（0）；
2、M6311模块：串口选择（串口2），波特率（115200），中断优先级（2），响应优先级（0）；
3、液晶屏模块：串口选择（串口4），波特率（9600），中断优先级（3），响应优先级（1）；
4、握手定时器：中断优先级（1），响应优先级（1）；
5、57卡：中断优先级（1），响应优先级（0）；
6、24.42卡：中断优先级（1），响应优先级（0）；

存在问题
1、信号强度不会实时改变；
2、写卡字符有问题；
3、测试强度不够；